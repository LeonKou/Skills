# 编码智能体发现协议

项目根目录 `AGENTS.md` 是入口。任何编码任务开始前，智能体必须读取 `SYSTEM_BLUEPRINT.md`、`conventions.md`、`decisions.md` 和相关追踪/未决文件。`REQUIRED` 规则是项目约束；遇到冲突先提报，不得静默绕过。实现后更新追踪矩阵和变更记录。若发现可跨项目复用的新规范，提出回写 Skill 的建议并等待用户单独确认。
